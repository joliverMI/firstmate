### POST-FIX (this change): update follows the configured upstream (fork/main)

== fixture ==
origin/main  (public template) : 776ef1b  docs: template-only change (never on the fork)
fork/main    (we develop here) : 0a038c0  no-mistakes(review): resolve fleet-sync firstmate base, surface fallback notes
remote code root main tracks   : fork/main
remote code root HEAD          : 4f0a4f5
remote secondmate home HEAD    : 4f0a4f5

-- before the update, in the remote secondmate home --
bin/fm-dashboard.sh : ABSENT
fm-spawn.sh --card  : ABSENT

== $ fm-remote-secondmate-control.sh update ios ==
synced: 0a038c0ec5895ae98995b1a79ed35d248619f844
(exit 0)

-- after the update, in the remote secondmate home --
remote code root HEAD          : 0a038c0
remote secondmate home HEAD    : 0a038c0
bin/fm-dashboard.sh : PRESENT
fm-spawn.sh --card  : PRESENT

-- the card-link tooling actually runs in the remote home --
fm-dashboard.sh - the ONLY way an agent touches the Admiral's Fleet Dashboard.

The dashboard is a purpose-built task board, not a mirror of any backlog:
it owns its own persistent records (bin/fleet-dashboard/server/store.py),
